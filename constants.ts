
export const SYSTEM_DEFAULTS = {
  temperature: 0.1,
  topP: 0.95,
  topK: 64
};

// Lưu ý: FIELD_LABELS trong types.ts vẫn giữ để hiển thị tên tiếng Việt cho các key thông dụng.
// Nếu bạn dùng key mới hoàn toàn trong Schema, App sẽ hiển thị chính tên Key đó.
